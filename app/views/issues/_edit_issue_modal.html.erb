<!-- Modal -->
<div class="modal fade" id=<%= "editIssue_#{issue.id}" %> tabindex="-1" role="dialog" aria-labelledby=<%= "editIssue_#{issue.id}Title" %> aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id=<%= "editIssue_#{issue.id}Title" %>>Edit Issue</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <%= render 'issues/form', issue: issue %>
      </div>
    </div>
  </div>
</div>
